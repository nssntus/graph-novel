# GraphNovel Agent Working Agreement

## 项目定位

GraphNovel（番茄小说工坊）是一个基于 **Graph Engineering** 的 AI 辅助网文写作系统。它不是一组彼此独立的 Prompt，也不应退化为由页面拼接的串行脚本。

项目的核心语义是：

- **Node**：世界观、人物、大纲、章节规划、写作、审稿、润色、人工审批和全局终审等专用 Agent。
- **State**：`GraphNovelState` 是跨节点共享、持久化的唯一事实来源。
- **Edge**：引擎根据节点结果、审核决定和质量分数选择后续路径。
- **Gate**：人工审批是图中的正式暂停点。
- **Loop**：驳回和低分重写是有上限、有反馈上下文的受控循环。
- **Checkpoint**：关键状态转换后应可保存，并能在进程重启后恢复。

任何架构改动都必须保留或强化这些语义。

## 事实来源与开始工作前的检查

开始修改前，按以下顺序理解上下文：

1. 阅读本文件和与任务直接相关的源码。
2. 阅读 `HANDOVER.md` 了解历史背景，但将它视为交接记录而非绝对事实。
3. 查看现有测试和可执行验证方式。
4. 如目录是 Git 工作树，检查当前改动并保留用户的无关修改；如果不是，明确说明缺少版本基线。

源码、可复现行为和测试结果优先于交接文档中的陈述。对模型名称、外部 API 能力等可能变化的信息，必须实际核验或标记为未核验。

## 架构边界

- `graph_novel/state.py`：数据模型、状态序列化和恢复。
- `graph_novel/engine.py`：图调度、阶段转换、条件边、重写循环和输出编排。
- `graph_novel/nodes/`：各节点内部的 LLM 任务与节点级状态更新。
- `graph_novel/llm.py`：模型客户端和调用封装。
- `graph_novel/web/app.py`：HTTP 接口、项目加载和用户操作适配。
- `graph_novel/web/templates/`、`static/`：展示与交互，不承载图调度规则。
- `tests/test_integration.py`：当前基础回归套件。

路由和模板可以触发图事件，但不应自行复制节点调度、状态转换或重写规则。跨节点流程应由引擎统一负责。

## Graph Engineering 不变量

修改图相关代码时遵守以下约束：

1. `GraphNovelState` 是唯一事实来源，避免另建相互冲突的业务状态。
2. 节点执行状态必须明确经历 `PENDING`、`IN_PROGRESS`、`COMPLETED`、`FAILED` 或 `SKIPPED`；失败不能伪装成成功。
3. 节点只负责自己的业务职责；路由决策由引擎负责。
4. 人工审批应表现为“保存 checkpoint → 暂停 → 接收决定 → 沿对应边恢复”，不能长期阻塞 HTTP 请求。
5. 重写循环必须有明确上限，并把审稿问题或人工反馈传入下一轮。
6. 重写既有章节时，不能无意清除人工反馈、审批记录或仍需保留的历史产物。
7. 每个条件边都应有可测试的触发条件、成功条件和停止条件。
8. State 序列化格式发生变化时，考虑旧项目文件的兼容读取；不要无提示破坏已有存档。
9. 章节号在现有模型中从 1 开始；所有索引转换都要验证边界。
10. Web 进程重启后，已持久化项目应仍可加载；不要把内存字典当作长期可靠存储。

## 修改原则

- 先复现或定位，再做最小改动，然后运行相关验证。
- 优先修复根因，不以吞掉异常、伪造成功响应或写入占位正文掩盖失败。
- 不做与当前任务无关的重构、格式化、依赖升级或 UI 改版。
- 保持现有模块职责；新增抽象前先确认它至少解决一个真实、已验证的问题。
- 改动 API 时同步检查模板或 JavaScript 调用方，以及状态持久化行为。
- 改动 State 字段时同步检查序列化、反序列化、节点、Web API、模板和测试。
- Prompt 属于业务逻辑。修改 Prompt 时说明目标，并用固定 Mock 或结构断言验证输出契约。

## Python 与代码风格

- 目标运行时为 Python 3.9。
- 为匹配现有代码，类型注解优先使用 `Optional`、`List`、`Dict` 和 `Tuple`。
- 不使用 Python 3.10 才支持的 `X | None` 联合类型语法。
- 保持 dataclass 模型和现有导入风格，除非任务明确要求迁移。
- 用户可见文案默认使用中文；内部标识符沿用现有英文命名。
- 不引入新依赖，除非标准库和现有依赖无法合理完成任务，并已说明必要性。

## LLM 与外部服务

- 所有模型调用应通过 `graph_novel/llm.py` 或明确的统一适配层完成。
- 单元和集成测试默认 Mock LLM；不要让常规测试产生真实 API 费用。
- 不假设配置中的模型名称当前一定可用。真实调用前核对环境和供应商能力。
- 对 LLM 输出执行结构校验；解析失败应产生可观察的失败状态，而不是继续下游节点。
- 不在日志、异常、测试夹具或提交内容中输出 API Key。
- 不读取、修改或提交 `.env`，除非用户明确要求；示例配置只放占位值。

## Web 与并发

- 耗时节点不能占用请求线程等待人工审批。
- 同一项目的并发运行必须有明确策略，至少避免重复点击导致两个执行流同时覆盖 State。
- API 的 `success` 必须反映实际执行结果；超时、节点失败和仍在运行应返回不同状态。
- 对 JSON 请求体、项目 ID、章节号和表单数字做边界验证。
- 默认开发密钥仅限本地开发，不得视为生产配置。
- 测试涉及项目存储时，优先将 `GRAPH_NOVEL_DIR` 指向临时目录，避免污染用户目录。

## 验证

基础回归命令：

```bash
python3 tests/test_integration.py
```

启动本地 Web：

```bash
python3 web_server.py
```

根据改动范围补充最小验证：

- State 改动：保存、重新加载并比较关键嵌套字段和 Enum。
- Engine 改动：覆盖成功、失败、驳回、重写上限和恢复路径。
- Node 改动：在节点模块的调用位置 Mock `call_llm_sync`，覆盖合法与畸形响应。
- Web 改动：使用 Flask test client 验证状态码、JSON 契约和持久化结果。
- 前端改动：至少核对触发按钮、请求路径、加载态、错误态和恢复后的页面状态。

没有运行真实 API 时，必须明确写“Mock 流程通过，真实模型调用未验证”。验证失败时记录命令、错误和影响，不把任务表述为已完成。

## 安全与数据

- 小说项目、正文、人物设定和创作笔记属于用户数据，仅在当前任务所需范围内读取和处理。
- 不扩大目录权限，不读取凭据，不对外发布内容。
- 删除或覆盖项目存档、章节正文、输出文件前必须确认准确目标和用户意图。
- 默认保留已有项目文件；迁移应先备份或提供可回退方案。

## 完成交接格式

交付修改时先报告结果，再简要说明：

1. 改了什么。
2. 运行了哪些验证及结果。
3. 真实 API 是否验证。
4. 仍有哪些已知风险或下一步。

达到已定义的成功条件后停止，不重复运行已经通过且没有新信息的验证。
